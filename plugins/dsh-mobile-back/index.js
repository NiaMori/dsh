/**
 * dsh-mobile-back, node half. Pure client UI plugin: the empty apply exists so
 * the plugin appears in the host Loader; the browser half ships via
 * exports["./client"] and the package.json dsh.client declaration.
 */
export function apply() {}
